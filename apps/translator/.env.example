# Base URL of the broadcast listener SPA (used for shareable broadcast links + QR).
# In dev the broadcast app runs on its own port:
VITE_BROADCAST_URL=http://localhost:5177/broadcast

# Dev API proxy target (server). Defaults to http://localhost:8080.
# CT_API_PROXY=http://localhost:8088
