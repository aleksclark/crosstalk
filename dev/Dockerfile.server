FROM golang:1.25-bookworm

RUN apt-get update && apt-get install -y --no-install-recommends \
    sqlite3 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

RUN mkdir -p /app/bin

CMD ["sh", "-c", "cd /app/server && go build -o /app/bin/ct-server ./cmd/ct-server && exec /app/bin/ct-server"]
