{
  "name": "crosstalk",
  "private": true,
  "type": "module",
  "scripts": {
    "api:generate:ts": "pnpm --filter @crosstalk/api-client generate",
    "lint": "oxlint apps packages"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "devDependencies": {
    "oxlint": "^1.73.0"
  }
}
